#ifndef ZECA
#define ZECA
//#include "Teaupla.hpp"

class Oueurj;
class Streumons;
class Teupors;

class Zeca{
  private:
    int X;
    int Y;
    char etat;//oueurj = J / streumons = s / remus = X / diams = $ / geurchars = * /teupors = p / vide =' '


  public:
    Zeca();
    Zeca(int x, int y, char e);
};

#endif
