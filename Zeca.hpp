#ifndef ZECA
#define ZECA

class Oueurj;
class Streumons;
class Teupors;

class Zeca{
  private:
    int X;
    int Y;
    //oueurj = j / streumons = s / remus = m / diams = d / geurchars = c /teupors = p
    char etat;

  public:
    Zeca(int x, int y, char e);


};

#endif
